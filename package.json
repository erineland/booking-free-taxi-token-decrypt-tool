{
  "name": "token-decrypt-util",
  "version": "2.0.0",
  "description": "A small utility for locally encrypting and decrypting tokens for taxi.booking.com",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "decrypt": "node ./src/v2/decrypt.js",
    "encrypt": "node ./src/v2/encrypt.js",
    "generate-qa-free-taxi-token": "npm run encrypt -- --file ./example-free-taxi-booking-data.json",
    "generate-dev-free-taxi-token": "npm run encrypt -- --file ./example-free-taxi-booking-data.json --iv 'testing!' --passphrase 'GEM Web Disco testing encryption' --env 'dev'",
    "test:watch": "jest --watch"
  },
  "keywords": [
    "util",
    "private",
    "taxi",
    "token",
    "decrypt"
  ],
  "author": "***REMOVED*** ***REMOVED***",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.8.0",
    "moment": "^2.24.0"
  },
  "dependencies": {
    "@bookingcom/bkng-crypto": "^3.1.0",
    "@rides/node-config": "^1.3.1",
    "yargs": "^13.3.0"
  }
}
